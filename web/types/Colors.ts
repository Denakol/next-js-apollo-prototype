import { ShirtColor } from '../api/graphql'

const Colors = {
  baseColors: ['red', 'blue', 'grey', 'white', 'pink', 'black'],
  shirtColors: [ShirtColor.Blue, ShirtColor.White, ShirtColor.Grey],
}

export { Colors }
